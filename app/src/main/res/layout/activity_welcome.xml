<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/activity_welcome"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.d2dindustries.pictawall.WelcomeActivity">

        <android.support.v4.view.ViewPager
            android:id ="@+id/welcome_view_pager"
            android:layout_height="match_parent"
            android:layout_width="match_parent">
        </android.support.v4.view.ViewPager>

        <android.support.design.widget.TabLayout
            android:id="@+id/tabDots"
            android:layout_alignParentBottom="true"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:tabBackground="@drawable/tab_selector"
            app:tabGravity="center"
            app:tabIndicatorHeight="0dp"
            android:layout_marginBottom="@dimen/tab_vertical_margin" />

        <Button
            android:id="@+id/getStartedButton"
            android:background="@drawable/button_round"
            android:text="Get Started"
            android:layout_width="wrap_content"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"
            android:textColor="@color/colorWhite"
            android:layout_marginBottom="@dimen/welcome_button_vertical_margin"
            android:paddingLeft="@dimen/welcome_button_margin"
            android:paddingRight="@dimen/welcome_button_margin"
            android:layout_height="@dimen/welcome_button_height"
            android:textAllCaps="false"/>

</RelativeLayout>